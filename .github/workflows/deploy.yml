name: Deploy React to GitHub Pages

on:
  push:
    branches:
      - main  # 監聽 main 分支，只要有 Push 就會觸發

permissions:
  contents: write # ⚠️ 重要：給機器人寫入權限，不然它無法更新 gh-pages

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout 程式碼 🛎️
        uses: actions/checkout@v4

      - name: 安裝與打包 (Install & Build) 🔧
        run: |
          npm install
          npm run build

      - name: 部署到 gh-pages 🚀
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          branch: gh-pages # 這是機器人要推送的目的地分支
          folder: dist     # 👈【關鍵】這裡要注意！請看下方的說明
